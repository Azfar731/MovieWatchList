
Things left to do:
find a good cover for the header
fix API key/ENV variablel issue in the project
add a placeholder image in movieDetails Page.
Create a Readme
Add a path for /watchlist/movie-id. Maybe rename, /search/:search-title/:id to /movies/:id. So it can be used for both search page and watchlsit.

For later:
Add suspense and use deferredData
